# قرارداد عامل‌های هوش مصنوعی

این مخزن منبع معتبر مدیریت و گزارش پروژه‌ها است. حافظه گفتگو جایگزین اسناد این مخزن نیست.

## شروع هر تسک

1. `README.md`، `docs/PROJECT-MANAGEMENT.md` و README پروژه هدف را کامل بخوان.
2. README حوزه مرتبط در `workstreams/` و اسناد ارجاع‌شده از آن را بخوان.
3. وضعیت واقعی Git و تغییرات موجود را بررسی کن؛ تغییرات نامرتبط را دست نزن.
4. اگر اطلاعات لازم در مخزن نیست، آن را حدس نزن و به‌عنوان «نیازمند اطلاعات» ثبت کن.

## محدوده این مخزن

- این مخزن برای برنامه، وضعیت، تصمیم، گزارش، داده مدیریتی و خروجی تحقیق است.
- کد پروژه‌هایی که مخزن مستقل دارند در اینجا کپی نمی‌شود؛ فقط لینک، وضعیت و اثر مدیریتی آن‌ها ثبت می‌شود.
- رمز، توکن، اطلاعات ورود، فایل خصوصی مشتری و داده شخصی را ثبت نکن.
- فایل حجیم و خروجی قابل بازتولید را بدون نیاز روشن وارد Git نکن.

## ساختار اجباری

- هر پروژه: `projects/<slug>/README.md`
- هر حوزه: `projects/<slug>/workstreams/<area>/README.md`
- داده: `projects/<slug>/data/`
- گزارش تاریخ‌دار: `projects/<slug>/workstreams/<area>/reports/YYYY-MM-DD-<slug>.md`
- تصمیم مهم و ماندگار: `projects/<slug>/decisions/YYYY-MM-DD-<slug>.md`
- پروژه و حوزه جدید باید از الگوهای `templates/` ساخته شود.

نام مسیرها انگلیسی، lowercase و kebab-case باشد. متن اسناد می‌تواند فارسی باشد و باید UTF-8 سالم بماند.

## وضعیت‌ها و صداقت گزارش

فقط این وضعیت‌ها مجازند: `idea`، `planned`، `ready`، `in-progress`، `blocked`، `done`، `cancelled` و `monitoring`.

- `done` فقط وقتی ثبت شود که معیار پایان برآورده و نتیجه یا شاهد موجود باشد.
- برنامه، تحقیق ناقص، تغییر mergeنشده یا خروجی تست‌نشده به‌عنوان انجام‌شده گزارش نشود.
- اگر نتیجه با انتظار متفاوت است، نتیجه واقعی ثبت شود.
- تاریخ واقعی با قالب `DD/MM/YYYY` نوشته شود.
- آمار باید منبع، تاریخ برداشت و محدودیت روش داشته باشد.

## قرارداد اجرای تسک

1. هدف، محدوده و معیار پایان را در حوزه مرتبط مشخص کن.
2. فقط اسناد و داده‌های لازم همان تسک را تغییر بده.
3. نتیجه، شاهد، ریسک و اقدام بعدی را ثبت کن.
4. README پروژه را به‌روزرسانی کن.
5. اگر نتیجه روی نمای کل سبد پروژه‌ها اثر دارد، README ریشه را هم به‌روزرسانی کن.
6. تغییرات را روی branch با الگوی `agent/<project>-<task>` انجام بده، Pull Request بساز و پس از بررسی سازگاری اسناد merge کن.

## قرارداد لاگ README

- فعالیت‌ها از جدید به قدیم مرتب شوند.
- هر تسک فقط یک ردیف داشته باشد.
- قالب ردیف: `- \`DD/MM/YYYY\` — **عنوان کوتاه:** نتیجه واقعی و کوتاه.`
- کار آینده وارد لاگ فعالیت‌های انجام‌شده نشود و فقط در Roadmap یا Backlog بماند.
- جزئیات فنی و گزارش طولانی در README ریشه نوشته نشود؛ README فقط نمای مدیریتی است.

## قواعد سئو و محتوا

- هدف محتوا باید intent، صفحه مقصد، مخاطب و معیار موفقیت مشخص داشته باشد.
- محتوای انبوه، عمومی یا تولیدشده بدون داده و تجربه واقعی منتشر نشود.
- ادعای فنی، سازگاری قطعه، کد مرجع، قیمت و شرایط فروش حدس زده نشود.
- برای هر صفحه جدید، خطر هم‌پوشانی کلیدواژه و مسیر لینک‌سازی داخلی بررسی شود.
- داده‌های Search Console و Analytics پس از اتصال، بر حدس و ابزارهای تخمین مقدم‌اند.

## پایان هر تسک

گزارش نهایی باید شامل خلاصه تغییر، فایل‌های تغییرکرده، branch/PR/commit، آزمون یا اعتبارسنجی، موارد عمداً دست‌نخورده، ریسک باقی‌مانده و اقدام بعدی باشد.
